const BlabberCommand = require('./blabberCommand');

class IgnoreCommand extends BlabberCommand {
    constructor(cursor, username) {
        logger.info("Ignore command");
        super();
        
    }
}